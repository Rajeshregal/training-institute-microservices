server:
  port: 8083

spring:
  application:
    name: teacher-service

  datasource:
    url: jdbc:postgresql://localhost:5432/training_institute_db
    username: postgres
    password: system
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

